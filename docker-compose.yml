version: '3.8'

services:
  backend:
    build: ./backend
    container_name: embeddings-backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build: ./frontend
    container_name: embeddings-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
